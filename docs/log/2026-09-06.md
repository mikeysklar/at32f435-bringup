# 2026-09-06 session log

- Board arrived: AT-START-F435 (AT32F435ZMT7). Plugged into OTG1 (CN2) only; nothing
  enumerates on bravo or the Mac. Expected: AT-Link-EZ only powers from its own CN6 jack,
  and the factory firmware is not a known USB device on OTG1. Asked for CN6 -> bravo.
- Research done (see docs/research-*.md): TinyUSB, Zephyr, tooling.
- Created this repo, labels, milestones. Created `at32f435` branches on the
  circuitpython (adafruit main b6ebf3d), tinyusb (hathach master d9fa95d) and zephyr
  (adafruit/zephyr 499310d, the zephyr-cp pin) forks.
- bravo: probe-rs 0.32.0 installed to ~/.cargo/bin. ArteryTek/openocd cloning+building
  into ~/.local/openocd-at32. tinyusb fork cloned to ~/at32/tinyusb on at32f435 with
  Artery deps fetched. ArteryTek zephyr fork west workspace at ~/at32/zephyr-at32-ws.
- Decision: device USB on OTG1 (CN2, PA11/PA12) for all stacks.
- TinyUSB `cdc_msc` builds for at_start_f435 on bravo (23592 B flash, 11248 B RAM).
- ArteryTek/openocd fork built (`--disable-werror` needed on GCC 13) into
  `~/.local/openocd-at32`; has `target/at32f435xM.cfg`, `interface/atlink_dap_v2.cfg`.
- Fork PR mikeysklar/tinyusb#1: RHPORT_DEVICE/RHPORT_HOST override for at32f435_437
  (branch `tinyusb-rhport-override`), verified in make + cmake, defaults unchanged.
- Branch `tinyusb-flash-target`: generic `flash-probe-rs` rule + at32f435_437 default,
  OPENOCD_OPTION for the fork. Dry-run only; needs hardware.
- Zephyr fork workspace `~/at32/zephyr-at32-ws` (west update done, 5.8 GB). Build needs
  Zephyr SDK 0.17.4 (bravo had 1.0.1 only); installing to `~/zephyr-sdk-0.17.4`.
- Still no AT-Link on the bus (waiting for CN6).
