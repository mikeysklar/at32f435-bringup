# 2026-09-06 session log

- Board arrived: AT-START-F435 (AT32F435ZMT7). Plugged into OTG1 (CN2) only; nothing
  enumerates on bravo or the Mac. Expected: AT-Link-EZ only powers from its own CN6 jack,
  and the factory firmware is not a known USB device on OTG1. Asked for CN6 -> bravo.
- Research done (see docs/research-*.md): TinyUSB, Zephyr, tooling.
- Created this repo, labels, milestones. Created `at32f435` branches on the
  circuitpython (adafruit main b6ebf3d), tinyusb (hathach master d9fa95d) and zephyr
  (adafruit/zephyr 499310d, the zephyr-cp pin) forks.
- bravo: probe-rs 0.32.0 installed to ~/.cargo/bin. ArteryTek/openocd cloning+building
  into ~/.local/openocd-at32. tinyusb fork cloned to ~/at32/tinyusb on at32f435 with
  Artery deps fetched. ArteryTek zephyr fork west workspace at ~/at32/zephyr-at32-ws.
- Decision: device USB on OTG1 (CN2, PA11/PA12) for all stacks.
- TinyUSB `cdc_msc` builds for at_start_f435 on bravo (23592 B flash, 11248 B RAM).
- ArteryTek/openocd fork built (`--disable-werror` needed on GCC 13) into
  `~/.local/openocd-at32`; has `target/at32f435xM.cfg`, `interface/atlink_dap_v2.cfg`.
- Fork PR mikeysklar/tinyusb#1: RHPORT_DEVICE/RHPORT_HOST override for at32f435_437
  (branch `tinyusb-rhport-override`), verified in make + cmake, defaults unchanged.
- Branch `tinyusb-flash-target`: generic `flash-probe-rs` rule + at32f435_437 default,
  OPENOCD_OPTION for the fork. Dry-run only; needs hardware.
- Zephyr fork workspace `~/at32/zephyr-at32-ws` (west update done, 5.8 GB). Build needs
  Zephyr SDK 0.17.4 (bravo had 1.0.1 only); installing to `~/zephyr-sdk-0.17.4`.
- Still no AT-Link on the bus (waiting for CN6).
- Zephyr SDK 0.17.4 (arm only) installed at `~/zephyr-sdks/zephyr-sdk-0.17.4` (west sdk
  install with `-d $HOME` fails on a /home temp dir; use a user-owned dir, then run
  `setup.sh -t arm-zephyr-eabi -c` by hand because the installer skipped it).
- Fork Zephyr builds: blinky 17596 B / 4384 B, cdc_acm 64284 B / 18160 B. cdc_acm uses
  the new stack (USB_DEVICE_STACK_NEXT) + udc_dwc2 with the fork's quirk block; DMA and
  HS Kconfigs are on by default but the FS core (ghwcfg2 0x228FDD00, slave-only) is
  detected at runtime. Board dts LEDs GPIO_ACTIVE_HIGH vs TinyUSB BSP active-low: verify.
- Fork dts flash partition table is broken (slot1 at 0x30000 overlaps slot0); irrelevant
  for samples, must be replaced for CIRCUITPY.
- Wrote docs/zephyr-module-inventory.md: the file list the out-of-tree module needs.
- TinyUSB `board_test` (LED blink + UART) built as the first flash image: 11864 B.
- zephyr-cp workspace on bravo at `~/at32/circuitpython/ports/zephyr-cp` (worktree of
  `~/circuitpython`, branch at32f435). Its west.yml pins adafruit/zephyr 52dc937c7
  (2026-09-03), not 499310d; moved the zephyr fork branch `at32f435` to 52dc937c7.
- Out-of-tree module https://github.com/mikeysklar/zephyr-at32-module (7 commits) builds
  blinky 16092 B, hello_world, flash_shell, tier-2 drivers, and cdc_acm 61472 B against
  Zephyr 4.4.99 with hal_at32 as a second extra module. Vendor prefix `artery`. Details
  and deviations in its README and issue #12.
- One core patch needed: compat-gated include in `drivers/usb/udc/udc_dwc2.h` ->
  mikeysklar/zephyr#27 (branch `zephyr-at32-dwc2-quirk`, PR into at32f435).
- Farm helpers in scripts/: `at32-find.sh` (sysfs path lookup), `at32-flash.sh`,
  `at32-backup.sh` (probe-rs). Cloned to `~/at32/bringup` on bravo.
- Agent now working on M4: zephyr-cp board `artery_at_start_f435` (issue #13).
